# Module: auth

Login, registration, email verification, password reset, session handling.

Feature-specific components/hooks/api calls for this module live in this
folder (`components/`, `hooks/`, `api/`), not in `src/shared`.
