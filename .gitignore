# .NET
bin/
obj/
*.user
*.suo
.vs/

# Node / frontend
node_modules/
dist/
dist-ssr/
frontend/.vite/
frontend/e2e-results/
.npm-cache/
.playwright-browsers/
*.local

# Environment
.env
.env.*
!.env.example

# Logs
*.log
logs/

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
*.swp
.claude/session-state.json

# Docker
docker-compose.override.yml

# Local scratch artifacts (e.g. tokens dumped ad hoc while debugging a session) — never a
# legitimate source file, so this narrow, explicit name is safe to ignore outright.
scratch_token.txt

# Playwright global-setup writes real session refresh tokens here for local storageState reuse —
# must never be committed.
frontend/e2e/.auth/
